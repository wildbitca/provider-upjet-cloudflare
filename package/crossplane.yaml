apiVersion: meta.pkg.crossplane.io/v1
kind: Provider
metadata:
  name: provider-cloudflare
  annotations:
    meta.crossplane.io/maintainer: "Wildbit"
    meta.crossplane.io/source: "https://github.com/wildbitca/provider-upjet-cloudflare"
    meta.crossplane.io/license: "Apache-2.0"
    meta.crossplane.io/description: "Upjet-based Crossplane provider for the Cloudflare API (Zones, DNS, Workers, Load Balancers, Zero Trust, and more)."
    meta.crossplane.io/readme: |
      provider-cloudflare exposes XRM-conformant managed resources for the Cloudflare API.
      See the source repository for full documentation, ProviderConfig setup, and supported resources.
spec:
  capabilities:
    - SafeStart
