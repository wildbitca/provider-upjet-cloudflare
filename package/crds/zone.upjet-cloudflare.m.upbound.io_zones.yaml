---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: zones.zone.upjet-cloudflare.m.upbound.io
spec:
  group: zone.upjet-cloudflare.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - upjet-cloudflare
    kind: Zone
    listKind: ZoneList
    plural: zones
    singular: zone
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Zone is the Schema for the Zones API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ZoneSpec defines the desired state of Zone
            properties:
              forProvider:
                properties:
                  account:
                    description: (Attributes) (see below for nested schema)
                    properties:
                      id:
                        description: |-
                          (String) Identifier
                          Identifier
                        type: string
                    type: object
                  name:
                    description: |-
                      (String) The domain name. Per RFC 1035 the overall zone name can be up to 253 characters, with each segment ("label") not exceeding 63 characters.
                      The domain name. Per [RFC 1035](https://datatracker.ietf.org/doc/html/rfc1035#section-2.3.4) the overall zone name can be up to 253 characters, with each segment ("label") not exceeding 63 characters.
                    type: string
                  paused:
                    description: |-
                      (Boolean) Indicates whether the zone is only using Cloudflare DNS services. A
                      true value means the zone will not receive security or performance
                      benefits.
                      Indicates whether the zone is only using Cloudflare DNS services. A
                      true value means the zone will not receive security or performance
                      benefits.
                    type: boolean
                  type:
                    description: |-
                      hosted zone or a CNAME setup.
                      Available values: "full", "partial", "secondary", "internal".
                      A full zone implies that DNS is hosted with Cloudflare. A partial zone is
                      typically a partner-hosted zone or a CNAME setup.
                      Available values: "full", "partial", "secondary", "internal".
                    type: string
                  vanityNameServers:
                    description: |-
                      (List of String) An array of domains used for custom name servers. This is only
                      available for Business and Enterprise plans.
                      An array of domains used for custom name servers. This is only
                      available for Business and Enterprise plans.
                    items:
                      type: string
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  account:
                    description: (Attributes) (see below for nested schema)
                    properties:
                      id:
                        description: |-
                          (String) Identifier
                          Identifier
                        type: string
                    type: object
                  name:
                    description: |-
                      (String) The domain name. Per RFC 1035 the overall zone name can be up to 253 characters, with each segment ("label") not exceeding 63 characters.
                      The domain name. Per [RFC 1035](https://datatracker.ietf.org/doc/html/rfc1035#section-2.3.4) the overall zone name can be up to 253 characters, with each segment ("label") not exceeding 63 characters.
                    type: string
                  paused:
                    description: |-
                      (Boolean) Indicates whether the zone is only using Cloudflare DNS services. A
                      true value means the zone will not receive security or performance
                      benefits.
                      Indicates whether the zone is only using Cloudflare DNS services. A
                      true value means the zone will not receive security or performance
                      benefits.
                    type: boolean
                  type:
                    description: |-
                      hosted zone or a CNAME setup.
                      Available values: "full", "partial", "secondary", "internal".
                      A full zone implies that DNS is hosted with Cloudflare. A partial zone is
                      typically a partner-hosted zone or a CNAME setup.
                      Available values: "full", "partial", "secondary", "internal".
                    type: string
                  vanityNameServers:
                    description: |-
                      (List of String) An array of domains used for custom name servers. This is only
                      available for Business and Enterprise plans.
                      An array of domains used for custom name servers. This is only
                      available for Business and Enterprise plans.
                    items:
                      type: string
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.account is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.account)
                || (has(self.initProvider) && has(self.initProvider.account))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: ZoneStatus defines the observed state of Zone.
            properties:
              atProvider:
                properties:
                  account:
                    description: (Attributes) (see below for nested schema)
                    properties:
                      id:
                        description: |-
                          (String) Identifier
                          Identifier
                        type: string
                    type: object
                  activatedOn:
                    description: |-
                      (String) The last time proof of ownership was detected and the zone was made
                      active.
                      The last time proof of ownership was detected and the zone was made
                      active.
                    type: string
                  cnameSuffix:
                    description: |-
                      (String) Allows the customer to use a custom apex.
                      Tenants Only Configuration.
                      Allows the customer to use a custom apex.
                      *Tenants Only Configuration*.
                    type: string
                  createdOn:
                    description: |-
                      (String) When the zone was created.
                      When the zone was created.
                    type: string
                  developmentMode:
                    description: |-
                      (Number) The interval (in seconds) from when development mode expires
                      (positive integer) or last expired (negative integer) for the
                      domain. If development mode has never been enabled, this value is 0.
                      The interval (in seconds) from when development mode expires
                      (positive integer) or last expired (negative integer) for the
                      domain. If development mode has never been enabled, this value is 0.
                    type: number
                  id:
                    description: (String) Identifier
                    type: string
                  meta:
                    description: (Attributes) Metadata about the zone. (see below
                      for nested schema)
                    properties:
                      cdnOnly:
                        description: |-
                          (Boolean) The zone is only configured for CDN.
                          The zone is only configured for CDN.
                        type: boolean
                      customCertificateQuota:
                        description: |-
                          (Number) Number of Custom Certificates the zone can have.
                          Number of Custom Certificates the zone can have.
                        type: number
                      dnsOnly:
                        description: |-
                          (Boolean) The zone is only configured for DNS.
                          The zone is only configured for DNS.
                        type: boolean
                      foundationDns:
                        description: |-
                          (Boolean) The zone is setup with Foundation DNS.
                          The zone is setup with Foundation DNS.
                        type: boolean
                      pageRuleQuota:
                        description: |-
                          (Number) Number of Page Rules a zone can have.
                          Number of Page Rules a zone can have.
                        type: number
                      phishingDetected:
                        description: |-
                          (Boolean) The zone has been flagged for phishing.
                          The zone has been flagged for phishing.
                        type: boolean
                      step:
                        description: (Number)
                        type: number
                    type: object
                  modifiedOn:
                    description: |-
                      (String) When the zone was last modified.
                      When the zone was last modified.
                    type: string
                  name:
                    description: |-
                      (String) The domain name. Per RFC 1035 the overall zone name can be up to 253 characters, with each segment ("label") not exceeding 63 characters.
                      The domain name. Per [RFC 1035](https://datatracker.ietf.org/doc/html/rfc1035#section-2.3.4) the overall zone name can be up to 253 characters, with each segment ("label") not exceeding 63 characters.
                    type: string
                  nameServers:
                    description: |-
                      (List of String) The name servers Cloudflare assigns to a zone.
                      The name servers Cloudflare assigns to a zone.
                    items:
                      type: string
                    type: array
                  originalDnshost:
                    description: |-
                      (String) DNS host at the time of switching to Cloudflare.
                      DNS host at the time of switching to Cloudflare.
                    type: string
                  originalNameServers:
                    description: |-
                      (List of String) Original name servers before moving to Cloudflare.
                      Original name servers before moving to Cloudflare.
                    items:
                      type: string
                    type: array
                  originalRegistrar:
                    description: |-
                      (String) Registrar for the domain at the time of switching to Cloudflare.
                      Registrar for the domain at the time of switching to Cloudflare.
                    type: string
                  owner:
                    description: (Attributes) The owner of the zone. (see below for
                      nested schema)
                    properties:
                      id:
                        description: |-
                          (String) Identifier
                          Identifier
                        type: string
                      name:
                        description: |-
                          (String) The domain name. Per RFC 1035 the overall zone name can be up to 253 characters, with each segment ("label") not exceeding 63 characters.
                          Name of the owner.
                        type: string
                      type:
                        description: |-
                          hosted zone or a CNAME setup.
                          Available values: "full", "partial", "secondary", "internal".
                          The type of owner.
                        type: string
                    type: object
                  paused:
                    description: |-
                      (Boolean) Indicates whether the zone is only using Cloudflare DNS services. A
                      true value means the zone will not receive security or performance
                      benefits.
                      Indicates whether the zone is only using Cloudflare DNS services. A
                      true value means the zone will not receive security or performance
                      benefits.
                    type: boolean
                  permissions:
                    description: |-
                      (List of String, Deprecated) Legacy permissions based on legacy user membership information.
                      Legacy permissions based on legacy user membership information.
                    items:
                      type: string
                    type: array
                  plan:
                    description: (Attributes, Deprecated) A Zones subscription information.
                      (see below for nested schema)
                    properties:
                      canSubscribe:
                        description: |-
                          (Boolean) States if the subscription can be activated.
                          States if the subscription can be activated.
                        type: boolean
                      currency:
                        description: |-
                          (String) The denomination of the customer.
                          The denomination of the customer.
                        type: string
                      externallyManaged:
                        description: |-
                          (Boolean) If this Zone is managed by another company.
                          If this Zone is managed by another company.
                        type: boolean
                      frequency:
                        description: |-
                          (String) How often the customer is billed.
                          How often the customer is billed.
                        type: string
                      id:
                        description: |-
                          (String) Identifier
                          Identifier
                        type: string
                      isSubscribed:
                        description: |-
                          (Boolean) States if the subscription active.
                          States if the subscription active.
                        type: boolean
                      legacyDiscount:
                        description: |-
                          (Boolean) If the legacy discount applies to this Zone.
                          If the legacy discount applies to this Zone.
                        type: boolean
                      legacyId:
                        description: |-
                          (String) The legacy name of the plan.
                          The legacy name of the plan.
                        type: string
                      name:
                        description: |-
                          (String) The domain name. Per RFC 1035 the overall zone name can be up to 253 characters, with each segment ("label") not exceeding 63 characters.
                          Name of the owner.
                        type: string
                      price:
                        description: |-
                          (Number) How much the customer is paying.
                          How much the customer is paying.
                        type: number
                    type: object
                  status:
                    description: |-
                      (String) The zone status on Cloudflare.
                      Available values: "initializing", "pending", "active", "moved".
                      The zone status on Cloudflare.
                      Available values: "initializing", "pending", "active", "moved".
                    type: string
                  tenant:
                    description: (Attributes) The root organizational unit that this
                      zone belongs to (such as a tenant or organization). (see below
                      for nested schema)
                    properties:
                      id:
                        description: |-
                          (String) Identifier
                          Identifier
                        type: string
                      name:
                        description: |-
                          (String) The domain name. Per RFC 1035 the overall zone name can be up to 253 characters, with each segment ("label") not exceeding 63 characters.
                          The name of the Tenant account.
                        type: string
                    type: object
                  tenantUnit:
                    description: organization). (see below for nested schema)
                    properties:
                      id:
                        description: |-
                          (String) Identifier
                          Identifier
                        type: string
                    type: object
                  type:
                    description: |-
                      hosted zone or a CNAME setup.
                      Available values: "full", "partial", "secondary", "internal".
                      A full zone implies that DNS is hosted with Cloudflare. A partial zone is
                      typically a partner-hosted zone or a CNAME setup.
                      Available values: "full", "partial", "secondary", "internal".
                    type: string
                  vanityNameServers:
                    description: |-
                      (List of String) An array of domains used for custom name servers. This is only
                      available for Business and Enterprise plans.
                      An array of domains used for custom name servers. This is only
                      available for Business and Enterprise plans.
                    items:
                      type: string
                    type: array
                  verificationKey:
                    description: |-
                      (String) Verification key for partial zone setup.
                      Verification key for partial zone setup.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
