---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: trusttunnelcloudflaredconfigs.zero.upjet-cloudflare.m.upbound.io
spec:
  group: zero.upjet-cloudflare.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - upjet-cloudflare
    kind: TrustTunnelCloudflaredConfig
    listKind: TrustTunnelCloudflaredConfigList
    plural: trusttunnelcloudflaredconfigs
    singular: trusttunnelcloudflaredconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TrustTunnelCloudflaredConfig is the Schema for the TrustTunnelCloudflaredConfigs
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TrustTunnelCloudflaredConfigSpec defines the desired state
              of TrustTunnelCloudflaredConfig
            properties:
              forProvider:
                properties:
                  accountId:
                    description: |-
                      (String) Identifier.
                      Identifier.
                    type: string
                  config:
                    description: (Attributes) The tunnel configuration and ingress
                      rules. (see below for nested schema)
                    properties:
                      ingress:
                        description: (Attributes List) List of public hostname definitions.
                          At least one ingress rule needs to be defined for the tunnel.
                          (see below for nested schema)
                        items:
                          properties:
                            hostname:
                              description: |-
                                (String) Public hostname for this service.
                                Public hostname for this service.
                              type: string
                            originRequest:
                              description: (Attributes) Configuration parameters for
                                the public hostname specific connection settings between
                                cloudflared and origin server. (see below for nested
                                schema)
                              properties:
                                access:
                                  description: Access-Jwt-Assertion request header.
                                    (see below for nested schema)
                                  properties:
                                    audTag:
                                      description: |-
                                        (List of String) Access applications that are allowed to reach this hostname for this Tunnel. Audience tags can be identified in the dashboard or via the List Access policies API.
                                        Access applications that are allowed to reach this hostname for this Tunnel. Audience tags can be identified in the dashboard or via the List Access policies API.
                                      items:
                                        type: string
                                      type: array
                                    required:
                                      description: |-
                                        (Boolean) Deny traffic that has not fulfilled Access authorization.
                                        Deny traffic that has not fulfilled Access authorization.
                                      type: boolean
                                    teamName:
                                      description: (String)
                                      type: string
                                  type: object
                                caPool:
                                  description: |-
                                    (String) Path to the certificate authority (CA) for the certificate of your origin. This option should be used only if your certificate is not signed by Cloudflare.
                                    Path to the certificate authority (CA) for the certificate of your origin. This option should be used only if your certificate is not signed by Cloudflare.
                                  type: string
                                connectTimeout:
                                  description: |-
                                    (Number) Timeout for establishing a new TCP connection to your origin server. This excludes the time taken to establish TLS, which is controlled by tlsTimeout.
                                    Timeout for establishing a new TCP connection to your origin server. This excludes the time taken to establish TLS, which is controlled by tlsTimeout.
                                  type: number
                                disableChunkedEncoding:
                                  description: |-
                                    (Boolean) Disables chunked transfer encoding. Useful if you are running a WSGI server.
                                    Disables chunked transfer encoding. Useful if you are running a WSGI server.
                                  type: boolean
                                http2Origin:
                                  description: |-
                                    (Boolean) Attempt to connect to origin using HTTP2. Origin must be configured as https.
                                    Attempt to connect to origin using HTTP2. Origin must be configured as https.
                                  type: boolean
                                httpHostHeader:
                                  description: |-
                                    (String) Sets the HTTP Host header on requests sent to the local service.
                                    Sets the HTTP Host header on requests sent to the local service.
                                  type: string
                                keepAliveConnections:
                                  description: |-
                                    (Number) Maximum number of idle keepalive connections between Tunnel and your origin. This does not restrict the total number of concurrent connections.
                                    Maximum number of idle keepalive connections between Tunnel and your origin. This does not restrict the total number of concurrent connections.
                                  type: number
                                keepAliveTimeout:
                                  description: |-
                                    (Number) Timeout after which an idle keepalive connection can be discarded.
                                    Timeout after which an idle keepalive connection can be discarded.
                                  type: number
                                matchSnItoHost:
                                  description: |-
                                    (Boolean) Auto configure the Hostname on the origin server certificate.
                                    Auto configure the Hostname on the origin server certificate.
                                  type: boolean
                                noHappyEyeballs:
                                  description: |-
                                    (Boolean) Disable the “happy eyeballs” algorithm for IPv4/IPv6 fallback if your local network has misconfigured one of the protocols.
                                    Disable the “happy eyeballs” algorithm for IPv4/IPv6 fallback if your local network has misconfigured one of the protocols.
                                  type: boolean
                                noTlsVerify:
                                  description: |-
                                    (Boolean) Disables TLS verification of the certificate presented by your origin. Will allow any certificate from the origin to be accepted.
                                    Disables TLS verification of the certificate presented by your origin. Will allow any certificate from the origin to be accepted.
                                  type: boolean
                                originServerName:
                                  description: |-
                                    (String) Hostname that cloudflared should expect from your origin server certificate.
                                    Hostname that cloudflared should expect from your origin server certificate.
                                  type: string
                                proxyType:
                                  description: |-
                                    (String) cloudflared starts a proxy server to translate HTTP traffic into TCP when proxying, for example, SSH or RDP. This configures what type of proxy will be started. Valid options are: "" for the regular proxy and "socks" for a SOCKS5 proxy.
                                    cloudflared starts a proxy server to translate HTTP traffic into TCP when proxying, for example, SSH or RDP. This configures what type of proxy will be started. Valid options are: "" for the regular proxy and "socks" for a SOCKS5 proxy.
                                  type: string
                                tcpKeepAlive:
                                  description: |-
                                    (Number) The timeout after which a TCP keepalive packet is sent on a connection between Tunnel and the origin server.
                                    The timeout after which a TCP keepalive packet is sent on a connection between Tunnel and the origin server.
                                  type: number
                                tlsTimeout:
                                  description: |-
                                    (Number) Timeout for completing a TLS handshake to your origin server, if you have chosen to connect Tunnel to an HTTPS server.
                                    Timeout for completing a TLS handshake to your origin server, if you have chosen to connect Tunnel to an HTTPS server.
                                  type: number
                              type: object
                            path:
                              description: |-
                                (String) Requests with this path route to this public hostname.
                                Requests with this path route to this public hostname.
                              type: string
                            service:
                              description: |-
                                (String) Protocol and address of destination server. Supported protocols: http://, https://, unix://, tcp://, ssh://, rdp://, unix+tls://, smb://. Alternatively can return a HTTP status code http_status:[code] e.g. 'http_status:404'.
                                Protocol and address of destination server. Supported protocols: http://, https://, unix://, tcp://, ssh://, rdp://, unix+tls://, smb://. Alternatively can return a HTTP status code http_status:[code] e.g. 'http_status:404'.
                              type: string
                          type: object
                        type: array
                      originRequest:
                        description: (Attributes) Configuration parameters for the
                          public hostname specific connection settings between cloudflared
                          and origin server. (see below for nested schema)
                        properties:
                          access:
                            description: Access-Jwt-Assertion request header. (see
                              below for nested schema)
                            properties:
                              audTag:
                                description: |-
                                  (List of String) Access applications that are allowed to reach this hostname for this Tunnel. Audience tags can be identified in the dashboard or via the List Access policies API.
                                  Access applications that are allowed to reach this hostname for this Tunnel. Audience tags can be identified in the dashboard or via the List Access policies API.
                                items:
                                  type: string
                                type: array
                              required:
                                description: |-
                                  (Boolean) Deny traffic that has not fulfilled Access authorization.
                                  Deny traffic that has not fulfilled Access authorization.
                                type: boolean
                              teamName:
                                description: (String)
                                type: string
                            type: object
                          caPool:
                            description: |-
                              (String) Path to the certificate authority (CA) for the certificate of your origin. This option should be used only if your certificate is not signed by Cloudflare.
                              Path to the certificate authority (CA) for the certificate of your origin. This option should be used only if your certificate is not signed by Cloudflare.
                            type: string
                          connectTimeout:
                            description: |-
                              (Number) Timeout for establishing a new TCP connection to your origin server. This excludes the time taken to establish TLS, which is controlled by tlsTimeout.
                              Timeout for establishing a new TCP connection to your origin server. This excludes the time taken to establish TLS, which is controlled by tlsTimeout.
                            type: number
                          disableChunkedEncoding:
                            description: |-
                              (Boolean) Disables chunked transfer encoding. Useful if you are running a WSGI server.
                              Disables chunked transfer encoding. Useful if you are running a WSGI server.
                            type: boolean
                          http2Origin:
                            description: |-
                              (Boolean) Attempt to connect to origin using HTTP2. Origin must be configured as https.
                              Attempt to connect to origin using HTTP2. Origin must be configured as https.
                            type: boolean
                          httpHostHeader:
                            description: |-
                              (String) Sets the HTTP Host header on requests sent to the local service.
                              Sets the HTTP Host header on requests sent to the local service.
                            type: string
                          keepAliveConnections:
                            description: |-
                              (Number) Maximum number of idle keepalive connections between Tunnel and your origin. This does not restrict the total number of concurrent connections.
                              Maximum number of idle keepalive connections between Tunnel and your origin. This does not restrict the total number of concurrent connections.
                            type: number
                          keepAliveTimeout:
                            description: |-
                              (Number) Timeout after which an idle keepalive connection can be discarded.
                              Timeout after which an idle keepalive connection can be discarded.
                            type: number
                          matchSnItoHost:
                            description: |-
                              (Boolean) Auto configure the Hostname on the origin server certificate.
                              Auto configure the Hostname on the origin server certificate.
                            type: boolean
                          noHappyEyeballs:
                            description: |-
                              (Boolean) Disable the “happy eyeballs” algorithm for IPv4/IPv6 fallback if your local network has misconfigured one of the protocols.
                              Disable the “happy eyeballs” algorithm for IPv4/IPv6 fallback if your local network has misconfigured one of the protocols.
                            type: boolean
                          noTlsVerify:
                            description: |-
                              (Boolean) Disables TLS verification of the certificate presented by your origin. Will allow any certificate from the origin to be accepted.
                              Disables TLS verification of the certificate presented by your origin. Will allow any certificate from the origin to be accepted.
                            type: boolean
                          originServerName:
                            description: |-
                              (String) Hostname that cloudflared should expect from your origin server certificate.
                              Hostname that cloudflared should expect from your origin server certificate.
                            type: string
                          proxyType:
                            description: |-
                              (String) cloudflared starts a proxy server to translate HTTP traffic into TCP when proxying, for example, SSH or RDP. This configures what type of proxy will be started. Valid options are: "" for the regular proxy and "socks" for a SOCKS5 proxy.
                              cloudflared starts a proxy server to translate HTTP traffic into TCP when proxying, for example, SSH or RDP. This configures what type of proxy will be started. Valid options are: "" for the regular proxy and "socks" for a SOCKS5 proxy.
                            type: string
                          tcpKeepAlive:
                            description: |-
                              (Number) The timeout after which a TCP keepalive packet is sent on a connection between Tunnel and the origin server.
                              The timeout after which a TCP keepalive packet is sent on a connection between Tunnel and the origin server.
                            type: number
                          tlsTimeout:
                            description: |-
                              (Number) Timeout for completing a TLS handshake to your origin server, if you have chosen to connect Tunnel to an HTTPS server.
                              Timeout for completing a TLS handshake to your origin server, if you have chosen to connect Tunnel to an HTTPS server.
                            type: number
                        type: object
                    type: object
                  source:
                    description: |-
                      (String) Indicates if this is a locally or remotely configured tunnel. If local, manage the tunnel using a YAML file on the origin machine. If cloudflare, manage the tunnel's configuration on the Zero Trust dashboard.
                      Available values: "local", "cloudflare".
                      Indicates if this is a locally or remotely configured tunnel. If `local`, manage the tunnel using a YAML file on the origin machine. If `cloudflare`, manage the tunnel's configuration on the Zero Trust dashboard.
                      Available values: "local", "cloudflare".
                    type: string
                  tunnelId:
                    description: |-
                      (String) UUID of the tunnel.
                      UUID of the tunnel.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accountId:
                    description: |-
                      (String) Identifier.
                      Identifier.
                    type: string
                  config:
                    description: (Attributes) The tunnel configuration and ingress
                      rules. (see below for nested schema)
                    properties:
                      ingress:
                        description: (Attributes List) List of public hostname definitions.
                          At least one ingress rule needs to be defined for the tunnel.
                          (see below for nested schema)
                        items:
                          properties:
                            hostname:
                              description: |-
                                (String) Public hostname for this service.
                                Public hostname for this service.
                              type: string
                            originRequest:
                              description: (Attributes) Configuration parameters for
                                the public hostname specific connection settings between
                                cloudflared and origin server. (see below for nested
                                schema)
                              properties:
                                access:
                                  description: Access-Jwt-Assertion request header.
                                    (see below for nested schema)
                                  properties:
                                    audTag:
                                      description: |-
                                        (List of String) Access applications that are allowed to reach this hostname for this Tunnel. Audience tags can be identified in the dashboard or via the List Access policies API.
                                        Access applications that are allowed to reach this hostname for this Tunnel. Audience tags can be identified in the dashboard or via the List Access policies API.
                                      items:
                                        type: string
                                      type: array
                                    required:
                                      description: |-
                                        (Boolean) Deny traffic that has not fulfilled Access authorization.
                                        Deny traffic that has not fulfilled Access authorization.
                                      type: boolean
                                    teamName:
                                      description: (String)
                                      type: string
                                  type: object
                                caPool:
                                  description: |-
                                    (String) Path to the certificate authority (CA) for the certificate of your origin. This option should be used only if your certificate is not signed by Cloudflare.
                                    Path to the certificate authority (CA) for the certificate of your origin. This option should be used only if your certificate is not signed by Cloudflare.
                                  type: string
                                connectTimeout:
                                  description: |-
                                    (Number) Timeout for establishing a new TCP connection to your origin server. This excludes the time taken to establish TLS, which is controlled by tlsTimeout.
                                    Timeout for establishing a new TCP connection to your origin server. This excludes the time taken to establish TLS, which is controlled by tlsTimeout.
                                  type: number
                                disableChunkedEncoding:
                                  description: |-
                                    (Boolean) Disables chunked transfer encoding. Useful if you are running a WSGI server.
                                    Disables chunked transfer encoding. Useful if you are running a WSGI server.
                                  type: boolean
                                http2Origin:
                                  description: |-
                                    (Boolean) Attempt to connect to origin using HTTP2. Origin must be configured as https.
                                    Attempt to connect to origin using HTTP2. Origin must be configured as https.
                                  type: boolean
                                httpHostHeader:
                                  description: |-
                                    (String) Sets the HTTP Host header on requests sent to the local service.
                                    Sets the HTTP Host header on requests sent to the local service.
                                  type: string
                                keepAliveConnections:
                                  description: |-
                                    (Number) Maximum number of idle keepalive connections between Tunnel and your origin. This does not restrict the total number of concurrent connections.
                                    Maximum number of idle keepalive connections between Tunnel and your origin. This does not restrict the total number of concurrent connections.
                                  type: number
                                keepAliveTimeout:
                                  description: |-
                                    (Number) Timeout after which an idle keepalive connection can be discarded.
                                    Timeout after which an idle keepalive connection can be discarded.
                                  type: number
                                matchSnItoHost:
                                  description: |-
                                    (Boolean) Auto configure the Hostname on the origin server certificate.
                                    Auto configure the Hostname on the origin server certificate.
                                  type: boolean
                                noHappyEyeballs:
                                  description: |-
                                    (Boolean) Disable the “happy eyeballs” algorithm for IPv4/IPv6 fallback if your local network has misconfigured one of the protocols.
                                    Disable the “happy eyeballs” algorithm for IPv4/IPv6 fallback if your local network has misconfigured one of the protocols.
                                  type: boolean
                                noTlsVerify:
                                  description: |-
                                    (Boolean) Disables TLS verification of the certificate presented by your origin. Will allow any certificate from the origin to be accepted.
                                    Disables TLS verification of the certificate presented by your origin. Will allow any certificate from the origin to be accepted.
                                  type: boolean
                                originServerName:
                                  description: |-
                                    (String) Hostname that cloudflared should expect from your origin server certificate.
                                    Hostname that cloudflared should expect from your origin server certificate.
                                  type: string
                                proxyType:
                                  description: |-
                                    (String) cloudflared starts a proxy server to translate HTTP traffic into TCP when proxying, for example, SSH or RDP. This configures what type of proxy will be started. Valid options are: "" for the regular proxy and "socks" for a SOCKS5 proxy.
                                    cloudflared starts a proxy server to translate HTTP traffic into TCP when proxying, for example, SSH or RDP. This configures what type of proxy will be started. Valid options are: "" for the regular proxy and "socks" for a SOCKS5 proxy.
                                  type: string
                                tcpKeepAlive:
                                  description: |-
                                    (Number) The timeout after which a TCP keepalive packet is sent on a connection between Tunnel and the origin server.
                                    The timeout after which a TCP keepalive packet is sent on a connection between Tunnel and the origin server.
                                  type: number
                                tlsTimeout:
                                  description: |-
                                    (Number) Timeout for completing a TLS handshake to your origin server, if you have chosen to connect Tunnel to an HTTPS server.
                                    Timeout for completing a TLS handshake to your origin server, if you have chosen to connect Tunnel to an HTTPS server.
                                  type: number
                              type: object
                            path:
                              description: |-
                                (String) Requests with this path route to this public hostname.
                                Requests with this path route to this public hostname.
                              type: string
                            service:
                              description: |-
                                (String) Protocol and address of destination server. Supported protocols: http://, https://, unix://, tcp://, ssh://, rdp://, unix+tls://, smb://. Alternatively can return a HTTP status code http_status:[code] e.g. 'http_status:404'.
                                Protocol and address of destination server. Supported protocols: http://, https://, unix://, tcp://, ssh://, rdp://, unix+tls://, smb://. Alternatively can return a HTTP status code http_status:[code] e.g. 'http_status:404'.
                              type: string
                          type: object
                        type: array
                      originRequest:
                        description: (Attributes) Configuration parameters for the
                          public hostname specific connection settings between cloudflared
                          and origin server. (see below for nested schema)
                        properties:
                          access:
                            description: Access-Jwt-Assertion request header. (see
                              below for nested schema)
                            properties:
                              audTag:
                                description: |-
                                  (List of String) Access applications that are allowed to reach this hostname for this Tunnel. Audience tags can be identified in the dashboard or via the List Access policies API.
                                  Access applications that are allowed to reach this hostname for this Tunnel. Audience tags can be identified in the dashboard or via the List Access policies API.
                                items:
                                  type: string
                                type: array
                              required:
                                description: |-
                                  (Boolean) Deny traffic that has not fulfilled Access authorization.
                                  Deny traffic that has not fulfilled Access authorization.
                                type: boolean
                              teamName:
                                description: (String)
                                type: string
                            type: object
                          caPool:
                            description: |-
                              (String) Path to the certificate authority (CA) for the certificate of your origin. This option should be used only if your certificate is not signed by Cloudflare.
                              Path to the certificate authority (CA) for the certificate of your origin. This option should be used only if your certificate is not signed by Cloudflare.
                            type: string
                          connectTimeout:
                            description: |-
                              (Number) Timeout for establishing a new TCP connection to your origin server. This excludes the time taken to establish TLS, which is controlled by tlsTimeout.
                              Timeout for establishing a new TCP connection to your origin server. This excludes the time taken to establish TLS, which is controlled by tlsTimeout.
                            type: number
                          disableChunkedEncoding:
                            description: |-
                              (Boolean) Disables chunked transfer encoding. Useful if you are running a WSGI server.
                              Disables chunked transfer encoding. Useful if you are running a WSGI server.
                            type: boolean
                          http2Origin:
                            description: |-
                              (Boolean) Attempt to connect to origin using HTTP2. Origin must be configured as https.
                              Attempt to connect to origin using HTTP2. Origin must be configured as https.
                            type: boolean
                          httpHostHeader:
                            description: |-
                              (String) Sets the HTTP Host header on requests sent to the local service.
                              Sets the HTTP Host header on requests sent to the local service.
                            type: string
                          keepAliveConnections:
                            description: |-
                              (Number) Maximum number of idle keepalive connections between Tunnel and your origin. This does not restrict the total number of concurrent connections.
                              Maximum number of idle keepalive connections between Tunnel and your origin. This does not restrict the total number of concurrent connections.
                            type: number
                          keepAliveTimeout:
                            description: |-
                              (Number) Timeout after which an idle keepalive connection can be discarded.
                              Timeout after which an idle keepalive connection can be discarded.
                            type: number
                          matchSnItoHost:
                            description: |-
                              (Boolean) Auto configure the Hostname on the origin server certificate.
                              Auto configure the Hostname on the origin server certificate.
                            type: boolean
                          noHappyEyeballs:
                            description: |-
                              (Boolean) Disable the “happy eyeballs” algorithm for IPv4/IPv6 fallback if your local network has misconfigured one of the protocols.
                              Disable the “happy eyeballs” algorithm for IPv4/IPv6 fallback if your local network has misconfigured one of the protocols.
                            type: boolean
                          noTlsVerify:
                            description: |-
                              (Boolean) Disables TLS verification of the certificate presented by your origin. Will allow any certificate from the origin to be accepted.
                              Disables TLS verification of the certificate presented by your origin. Will allow any certificate from the origin to be accepted.
                            type: boolean
                          originServerName:
                            description: |-
                              (String) Hostname that cloudflared should expect from your origin server certificate.
                              Hostname that cloudflared should expect from your origin server certificate.
                            type: string
                          proxyType:
                            description: |-
                              (String) cloudflared starts a proxy server to translate HTTP traffic into TCP when proxying, for example, SSH or RDP. This configures what type of proxy will be started. Valid options are: "" for the regular proxy and "socks" for a SOCKS5 proxy.
                              cloudflared starts a proxy server to translate HTTP traffic into TCP when proxying, for example, SSH or RDP. This configures what type of proxy will be started. Valid options are: "" for the regular proxy and "socks" for a SOCKS5 proxy.
                            type: string
                          tcpKeepAlive:
                            description: |-
                              (Number) The timeout after which a TCP keepalive packet is sent on a connection between Tunnel and the origin server.
                              The timeout after which a TCP keepalive packet is sent on a connection between Tunnel and the origin server.
                            type: number
                          tlsTimeout:
                            description: |-
                              (Number) Timeout for completing a TLS handshake to your origin server, if you have chosen to connect Tunnel to an HTTPS server.
                              Timeout for completing a TLS handshake to your origin server, if you have chosen to connect Tunnel to an HTTPS server.
                            type: number
                        type: object
                    type: object
                  source:
                    description: |-
                      (String) Indicates if this is a locally or remotely configured tunnel. If local, manage the tunnel using a YAML file on the origin machine. If cloudflare, manage the tunnel's configuration on the Zero Trust dashboard.
                      Available values: "local", "cloudflare".
                      Indicates if this is a locally or remotely configured tunnel. If `local`, manage the tunnel using a YAML file on the origin machine. If `cloudflare`, manage the tunnel's configuration on the Zero Trust dashboard.
                      Available values: "local", "cloudflare".
                    type: string
                  tunnelId:
                    description: |-
                      (String) UUID of the tunnel.
                      UUID of the tunnel.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.accountId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.accountId)
                || (has(self.initProvider) && has(self.initProvider.accountId))'
            - message: spec.forProvider.tunnelId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.tunnelId)
                || (has(self.initProvider) && has(self.initProvider.tunnelId))'
          status:
            description: TrustTunnelCloudflaredConfigStatus defines the observed state
              of TrustTunnelCloudflaredConfig.
            properties:
              atProvider:
                properties:
                  accountId:
                    description: |-
                      (String) Identifier.
                      Identifier.
                    type: string
                  config:
                    description: (Attributes) The tunnel configuration and ingress
                      rules. (see below for nested schema)
                    properties:
                      ingress:
                        description: (Attributes List) List of public hostname definitions.
                          At least one ingress rule needs to be defined for the tunnel.
                          (see below for nested schema)
                        items:
                          properties:
                            hostname:
                              description: |-
                                (String) Public hostname for this service.
                                Public hostname for this service.
                              type: string
                            originRequest:
                              description: (Attributes) Configuration parameters for
                                the public hostname specific connection settings between
                                cloudflared and origin server. (see below for nested
                                schema)
                              properties:
                                access:
                                  description: Access-Jwt-Assertion request header.
                                    (see below for nested schema)
                                  properties:
                                    audTag:
                                      description: |-
                                        (List of String) Access applications that are allowed to reach this hostname for this Tunnel. Audience tags can be identified in the dashboard or via the List Access policies API.
                                        Access applications that are allowed to reach this hostname for this Tunnel. Audience tags can be identified in the dashboard or via the List Access policies API.
                                      items:
                                        type: string
                                      type: array
                                    required:
                                      description: |-
                                        (Boolean) Deny traffic that has not fulfilled Access authorization.
                                        Deny traffic that has not fulfilled Access authorization.
                                      type: boolean
                                    teamName:
                                      description: (String)
                                      type: string
                                  type: object
                                caPool:
                                  description: |-
                                    (String) Path to the certificate authority (CA) for the certificate of your origin. This option should be used only if your certificate is not signed by Cloudflare.
                                    Path to the certificate authority (CA) for the certificate of your origin. This option should be used only if your certificate is not signed by Cloudflare.
                                  type: string
                                connectTimeout:
                                  description: |-
                                    (Number) Timeout for establishing a new TCP connection to your origin server. This excludes the time taken to establish TLS, which is controlled by tlsTimeout.
                                    Timeout for establishing a new TCP connection to your origin server. This excludes the time taken to establish TLS, which is controlled by tlsTimeout.
                                  type: number
                                disableChunkedEncoding:
                                  description: |-
                                    (Boolean) Disables chunked transfer encoding. Useful if you are running a WSGI server.
                                    Disables chunked transfer encoding. Useful if you are running a WSGI server.
                                  type: boolean
                                http2Origin:
                                  description: |-
                                    (Boolean) Attempt to connect to origin using HTTP2. Origin must be configured as https.
                                    Attempt to connect to origin using HTTP2. Origin must be configured as https.
                                  type: boolean
                                httpHostHeader:
                                  description: |-
                                    (String) Sets the HTTP Host header on requests sent to the local service.
                                    Sets the HTTP Host header on requests sent to the local service.
                                  type: string
                                keepAliveConnections:
                                  description: |-
                                    (Number) Maximum number of idle keepalive connections between Tunnel and your origin. This does not restrict the total number of concurrent connections.
                                    Maximum number of idle keepalive connections between Tunnel and your origin. This does not restrict the total number of concurrent connections.
                                  type: number
                                keepAliveTimeout:
                                  description: |-
                                    (Number) Timeout after which an idle keepalive connection can be discarded.
                                    Timeout after which an idle keepalive connection can be discarded.
                                  type: number
                                matchSnItoHost:
                                  description: |-
                                    (Boolean) Auto configure the Hostname on the origin server certificate.
                                    Auto configure the Hostname on the origin server certificate.
                                  type: boolean
                                noHappyEyeballs:
                                  description: |-
                                    (Boolean) Disable the “happy eyeballs” algorithm for IPv4/IPv6 fallback if your local network has misconfigured one of the protocols.
                                    Disable the “happy eyeballs” algorithm for IPv4/IPv6 fallback if your local network has misconfigured one of the protocols.
                                  type: boolean
                                noTlsVerify:
                                  description: |-
                                    (Boolean) Disables TLS verification of the certificate presented by your origin. Will allow any certificate from the origin to be accepted.
                                    Disables TLS verification of the certificate presented by your origin. Will allow any certificate from the origin to be accepted.
                                  type: boolean
                                originServerName:
                                  description: |-
                                    (String) Hostname that cloudflared should expect from your origin server certificate.
                                    Hostname that cloudflared should expect from your origin server certificate.
                                  type: string
                                proxyType:
                                  description: |-
                                    (String) cloudflared starts a proxy server to translate HTTP traffic into TCP when proxying, for example, SSH or RDP. This configures what type of proxy will be started. Valid options are: "" for the regular proxy and "socks" for a SOCKS5 proxy.
                                    cloudflared starts a proxy server to translate HTTP traffic into TCP when proxying, for example, SSH or RDP. This configures what type of proxy will be started. Valid options are: "" for the regular proxy and "socks" for a SOCKS5 proxy.
                                  type: string
                                tcpKeepAlive:
                                  description: |-
                                    (Number) The timeout after which a TCP keepalive packet is sent on a connection between Tunnel and the origin server.
                                    The timeout after which a TCP keepalive packet is sent on a connection between Tunnel and the origin server.
                                  type: number
                                tlsTimeout:
                                  description: |-
                                    (Number) Timeout for completing a TLS handshake to your origin server, if you have chosen to connect Tunnel to an HTTPS server.
                                    Timeout for completing a TLS handshake to your origin server, if you have chosen to connect Tunnel to an HTTPS server.
                                  type: number
                              type: object
                            path:
                              description: |-
                                (String) Requests with this path route to this public hostname.
                                Requests with this path route to this public hostname.
                              type: string
                            service:
                              description: |-
                                (String) Protocol and address of destination server. Supported protocols: http://, https://, unix://, tcp://, ssh://, rdp://, unix+tls://, smb://. Alternatively can return a HTTP status code http_status:[code] e.g. 'http_status:404'.
                                Protocol and address of destination server. Supported protocols: http://, https://, unix://, tcp://, ssh://, rdp://, unix+tls://, smb://. Alternatively can return a HTTP status code http_status:[code] e.g. 'http_status:404'.
                              type: string
                          type: object
                        type: array
                      originRequest:
                        description: (Attributes) Configuration parameters for the
                          public hostname specific connection settings between cloudflared
                          and origin server. (see below for nested schema)
                        properties:
                          access:
                            description: Access-Jwt-Assertion request header. (see
                              below for nested schema)
                            properties:
                              audTag:
                                description: |-
                                  (List of String) Access applications that are allowed to reach this hostname for this Tunnel. Audience tags can be identified in the dashboard or via the List Access policies API.
                                  Access applications that are allowed to reach this hostname for this Tunnel. Audience tags can be identified in the dashboard or via the List Access policies API.
                                items:
                                  type: string
                                type: array
                              required:
                                description: |-
                                  (Boolean) Deny traffic that has not fulfilled Access authorization.
                                  Deny traffic that has not fulfilled Access authorization.
                                type: boolean
                              teamName:
                                description: (String)
                                type: string
                            type: object
                          caPool:
                            description: |-
                              (String) Path to the certificate authority (CA) for the certificate of your origin. This option should be used only if your certificate is not signed by Cloudflare.
                              Path to the certificate authority (CA) for the certificate of your origin. This option should be used only if your certificate is not signed by Cloudflare.
                            type: string
                          connectTimeout:
                            description: |-
                              (Number) Timeout for establishing a new TCP connection to your origin server. This excludes the time taken to establish TLS, which is controlled by tlsTimeout.
                              Timeout for establishing a new TCP connection to your origin server. This excludes the time taken to establish TLS, which is controlled by tlsTimeout.
                            type: number
                          disableChunkedEncoding:
                            description: |-
                              (Boolean) Disables chunked transfer encoding. Useful if you are running a WSGI server.
                              Disables chunked transfer encoding. Useful if you are running a WSGI server.
                            type: boolean
                          http2Origin:
                            description: |-
                              (Boolean) Attempt to connect to origin using HTTP2. Origin must be configured as https.
                              Attempt to connect to origin using HTTP2. Origin must be configured as https.
                            type: boolean
                          httpHostHeader:
                            description: |-
                              (String) Sets the HTTP Host header on requests sent to the local service.
                              Sets the HTTP Host header on requests sent to the local service.
                            type: string
                          keepAliveConnections:
                            description: |-
                              (Number) Maximum number of idle keepalive connections between Tunnel and your origin. This does not restrict the total number of concurrent connections.
                              Maximum number of idle keepalive connections between Tunnel and your origin. This does not restrict the total number of concurrent connections.
                            type: number
                          keepAliveTimeout:
                            description: |-
                              (Number) Timeout after which an idle keepalive connection can be discarded.
                              Timeout after which an idle keepalive connection can be discarded.
                            type: number
                          matchSnItoHost:
                            description: |-
                              (Boolean) Auto configure the Hostname on the origin server certificate.
                              Auto configure the Hostname on the origin server certificate.
                            type: boolean
                          noHappyEyeballs:
                            description: |-
                              (Boolean) Disable the “happy eyeballs” algorithm for IPv4/IPv6 fallback if your local network has misconfigured one of the protocols.
                              Disable the “happy eyeballs” algorithm for IPv4/IPv6 fallback if your local network has misconfigured one of the protocols.
                            type: boolean
                          noTlsVerify:
                            description: |-
                              (Boolean) Disables TLS verification of the certificate presented by your origin. Will allow any certificate from the origin to be accepted.
                              Disables TLS verification of the certificate presented by your origin. Will allow any certificate from the origin to be accepted.
                            type: boolean
                          originServerName:
                            description: |-
                              (String) Hostname that cloudflared should expect from your origin server certificate.
                              Hostname that cloudflared should expect from your origin server certificate.
                            type: string
                          proxyType:
                            description: |-
                              (String) cloudflared starts a proxy server to translate HTTP traffic into TCP when proxying, for example, SSH or RDP. This configures what type of proxy will be started. Valid options are: "" for the regular proxy and "socks" for a SOCKS5 proxy.
                              cloudflared starts a proxy server to translate HTTP traffic into TCP when proxying, for example, SSH or RDP. This configures what type of proxy will be started. Valid options are: "" for the regular proxy and "socks" for a SOCKS5 proxy.
                            type: string
                          tcpKeepAlive:
                            description: |-
                              (Number) The timeout after which a TCP keepalive packet is sent on a connection between Tunnel and the origin server.
                              The timeout after which a TCP keepalive packet is sent on a connection between Tunnel and the origin server.
                            type: number
                          tlsTimeout:
                            description: |-
                              (Number) Timeout for completing a TLS handshake to your origin server, if you have chosen to connect Tunnel to an HTTPS server.
                              Timeout for completing a TLS handshake to your origin server, if you have chosen to connect Tunnel to an HTTPS server.
                            type: number
                        type: object
                    type: object
                  createdAt:
                    description: (String)
                    type: string
                  id:
                    description: (String) UUID of the tunnel.
                    type: string
                  source:
                    description: |-
                      (String) Indicates if this is a locally or remotely configured tunnel. If local, manage the tunnel using a YAML file on the origin machine. If cloudflare, manage the tunnel's configuration on the Zero Trust dashboard.
                      Available values: "local", "cloudflare".
                      Indicates if this is a locally or remotely configured tunnel. If `local`, manage the tunnel using a YAML file on the origin machine. If `cloudflare`, manage the tunnel's configuration on the Zero Trust dashboard.
                      Available values: "local", "cloudflare".
                    type: string
                  tunnelId:
                    description: |-
                      (String) UUID of the tunnel.
                      UUID of the tunnel.
                    type: string
                  version:
                    description: |-
                      (Number) The version of the Tunnel Configuration.
                      The version of the Tunnel Configuration.
                    type: number
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
