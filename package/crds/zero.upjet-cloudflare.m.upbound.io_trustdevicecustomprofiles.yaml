---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: trustdevicecustomprofiles.zero.upjet-cloudflare.m.upbound.io
spec:
  group: zero.upjet-cloudflare.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - upjet-cloudflare
    kind: TrustDeviceCustomProfile
    listKind: TrustDeviceCustomProfileList
    plural: trustdevicecustomprofiles
    singular: trustdevicecustomprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TrustDeviceCustomProfile is the Schema for the TrustDeviceCustomProfiles
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TrustDeviceCustomProfileSpec defines the desired state of
              TrustDeviceCustomProfile
            properties:
              forProvider:
                properties:
                  accountId:
                    description: (String)
                    type: string
                  allowModeSwitch:
                    description: |-
                      (Boolean) Whether to allow the user to switch WARP between modes.
                      Whether to allow the user to switch WARP between modes.
                    type: boolean
                  allowUpdates:
                    description: |-
                      (Boolean) Whether to receive update notifications when a new version of the client is available.
                      Whether to receive update notifications when a new version of the client is available.
                    type: boolean
                  allowedToLeave:
                    description: |-
                      (Boolean) Whether to allow devices to leave the organization.
                      Whether to allow devices to leave the organization.
                    type: boolean
                  autoConnect:
                    description: |-
                      (Number) The amount of time in seconds to reconnect after having been disabled.
                      The amount of time in seconds to reconnect after having been disabled.
                    type: number
                  captivePortal:
                    description: |-
                      (Number) Turn on the captive portal after the specified amount of time.
                      Turn on the captive portal after the specified amount of time.
                    type: number
                  description:
                    description: |-
                      (String) A description of the policy.
                      A description of the policy.
                    type: string
                  disableAutoFallback:
                    description: |-
                      (Boolean) If the dns_server field of a fallback domain is not present, the client will fall back to a best guess of the default/system DNS resolvers unless this policy option is set to true.
                      If the `dns_server` field of a fallback domain is not present, the client will fall back to a best guess of the default/system DNS resolvers unless this policy option is set to `true`.
                    type: boolean
                  enabled:
                    description: |-
                      (Boolean) Whether the policy will be applied to matching devices.
                      Whether the policy will be applied to matching devices.
                    type: boolean
                  exclude:
                    description: (Attributes List) List of routes excluded in the
                      WARP client's tunnel. Both 'exclude' and 'include' cannot be
                      set in the same request. (see below for nested schema)
                    items:
                      properties:
                        address:
                          description: |-
                            (String) The address in CIDR format to exclude from the tunnel. If address is present, host must not be present.
                            The address in CIDR format to exclude from the tunnel. If `address` is present, `host` must not be present.
                          type: string
                        description:
                          description: |-
                            (String) A description of the policy.
                            A description of the Split Tunnel item, displayed in the client UI.
                          type: string
                        host:
                          description: |-
                            (String) The domain name to exclude from the tunnel. If host is present, address must not be present.
                            The domain name to exclude from the tunnel. If `host` is present, `address` must not be present.
                          type: string
                      type: object
                    type: array
                  excludeOfficeIps:
                    description: |-
                      (Boolean) Whether to add Microsoft IPs to Split Tunnel exclusions.
                      Whether to add Microsoft IPs to Split Tunnel exclusions.
                    type: boolean
                  include:
                    description: (Attributes List) List of routes included in the
                      WARP client's tunnel. Both 'exclude' and 'include' cannot be
                      set in the same request. (see below for nested schema)
                    items:
                      properties:
                        address:
                          description: |-
                            (String) The address in CIDR format to exclude from the tunnel. If address is present, host must not be present.
                            The address in CIDR format to include in the tunnel. If `address` is present, `host` must not be present.
                          type: string
                        description:
                          description: |-
                            (String) A description of the policy.
                            A description of the Split Tunnel item, displayed in the client UI.
                          type: string
                        host:
                          description: |-
                            (String) The domain name to exclude from the tunnel. If host is present, address must not be present.
                            The domain name to include in the tunnel. If `host` is present, `address` must not be present.
                          type: string
                      type: object
                    type: array
                  lanAllowMinutes:
                    description: |-
                      (Number) The amount of time in minutes a user is allowed access to their LAN. A value of 0 will allow LAN access until the next WARP reconnection, such as a reboot or a laptop waking from sleep. Note that this field is omitted from the response if null or unset.
                      The amount of time in minutes a user is allowed access to their LAN. A value of 0 will allow LAN access until the next WARP reconnection, such as a reboot or a laptop waking from sleep. Note that this field is omitted from the response if null or unset.
                    type: number
                  lanAllowSubnetSize:
                    description: |-
                      (Number) The size of the subnet for the local access network. Note that this field is omitted from the response if null or unset.
                      The size of the subnet for the local access network. Note that this field is omitted from the response if null or unset.
                    type: number
                  match:
                    description: |-
                      (String) The wirefilter expression to match devices. Available values: "identity.email", "identity.groups.id", "identity.groups.name", "identity.groups.email", "identity.service_token_uuid", "identity.saml_attributes", "network", "os.name", "os.version".
                      The wirefilter expression to match devices. Available values: "identity.email", "identity.groups.id", "identity.groups.name", "identity.groups.email", "identity.service_token_uuid", "identity.saml_attributes", "network", "os.name", "os.version".
                    type: string
                  name:
                    description: |-
                      (String) The name of the device settings profile.
                      The name of the device settings profile.
                    type: string
                  precedence:
                    description: |-
                      (Number) The precedence of the policy. Lower values indicate higher precedence. Policies will be evaluated in ascending order of this field.
                      The precedence of the policy. Lower values indicate higher precedence. Policies will be evaluated in ascending order of this field.
                    type: number
                  registerInterfaceIpWithDns:
                    description: |-
                      premises DNS server.
                      Determines if the operating system will register WARP's local interface IP with your on-premises DNS server.
                    type: boolean
                  sccmVpnBoundarySupport:
                    description: |-
                      (Boolean) Determines whether the WARP client indicates to SCCM that it is inside a VPN boundary. (Windows only).
                      Determines whether the WARP client indicates to SCCM that it is inside a VPN boundary. (Windows only).
                    type: boolean
                  serviceModeV2:
                    description: (Attributes) (see below for nested schema)
                    properties:
                      mode:
                        description: |-
                          (String) The mode to run the WARP client under.
                          The mode to run the WARP client under.
                        type: string
                      port:
                        description: |-
                          (Number) The port number when used with proxy mode.
                          The port number when used with proxy mode.
                        type: number
                    type: object
                  supportUrl:
                    description: |-
                      (String) The URL to launch when the Send Feedback button is clicked.
                      The URL to launch when the Send Feedback button is clicked.
                    type: string
                  switchLocked:
                    description: |-
                      (Boolean) Whether to allow the user to turn off the WARP switch and disconnect the client.
                      Whether to allow the user to turn off the WARP switch and disconnect the client.
                    type: boolean
                  tunnelProtocol:
                    description: |-
                      (String) Determines which tunnel protocol to use.
                      Determines which tunnel protocol to use.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accountId:
                    description: (String)
                    type: string
                  allowModeSwitch:
                    description: |-
                      (Boolean) Whether to allow the user to switch WARP between modes.
                      Whether to allow the user to switch WARP between modes.
                    type: boolean
                  allowUpdates:
                    description: |-
                      (Boolean) Whether to receive update notifications when a new version of the client is available.
                      Whether to receive update notifications when a new version of the client is available.
                    type: boolean
                  allowedToLeave:
                    description: |-
                      (Boolean) Whether to allow devices to leave the organization.
                      Whether to allow devices to leave the organization.
                    type: boolean
                  autoConnect:
                    description: |-
                      (Number) The amount of time in seconds to reconnect after having been disabled.
                      The amount of time in seconds to reconnect after having been disabled.
                    type: number
                  captivePortal:
                    description: |-
                      (Number) Turn on the captive portal after the specified amount of time.
                      Turn on the captive portal after the specified amount of time.
                    type: number
                  description:
                    description: |-
                      (String) A description of the policy.
                      A description of the policy.
                    type: string
                  disableAutoFallback:
                    description: |-
                      (Boolean) If the dns_server field of a fallback domain is not present, the client will fall back to a best guess of the default/system DNS resolvers unless this policy option is set to true.
                      If the `dns_server` field of a fallback domain is not present, the client will fall back to a best guess of the default/system DNS resolvers unless this policy option is set to `true`.
                    type: boolean
                  enabled:
                    description: |-
                      (Boolean) Whether the policy will be applied to matching devices.
                      Whether the policy will be applied to matching devices.
                    type: boolean
                  exclude:
                    description: (Attributes List) List of routes excluded in the
                      WARP client's tunnel. Both 'exclude' and 'include' cannot be
                      set in the same request. (see below for nested schema)
                    items:
                      properties:
                        address:
                          description: |-
                            (String) The address in CIDR format to exclude from the tunnel. If address is present, host must not be present.
                            The address in CIDR format to exclude from the tunnel. If `address` is present, `host` must not be present.
                          type: string
                        description:
                          description: |-
                            (String) A description of the policy.
                            A description of the Split Tunnel item, displayed in the client UI.
                          type: string
                        host:
                          description: |-
                            (String) The domain name to exclude from the tunnel. If host is present, address must not be present.
                            The domain name to exclude from the tunnel. If `host` is present, `address` must not be present.
                          type: string
                      type: object
                    type: array
                  excludeOfficeIps:
                    description: |-
                      (Boolean) Whether to add Microsoft IPs to Split Tunnel exclusions.
                      Whether to add Microsoft IPs to Split Tunnel exclusions.
                    type: boolean
                  include:
                    description: (Attributes List) List of routes included in the
                      WARP client's tunnel. Both 'exclude' and 'include' cannot be
                      set in the same request. (see below for nested schema)
                    items:
                      properties:
                        address:
                          description: |-
                            (String) The address in CIDR format to exclude from the tunnel. If address is present, host must not be present.
                            The address in CIDR format to include in the tunnel. If `address` is present, `host` must not be present.
                          type: string
                        description:
                          description: |-
                            (String) A description of the policy.
                            A description of the Split Tunnel item, displayed in the client UI.
                          type: string
                        host:
                          description: |-
                            (String) The domain name to exclude from the tunnel. If host is present, address must not be present.
                            The domain name to include in the tunnel. If `host` is present, `address` must not be present.
                          type: string
                      type: object
                    type: array
                  lanAllowMinutes:
                    description: |-
                      (Number) The amount of time in minutes a user is allowed access to their LAN. A value of 0 will allow LAN access until the next WARP reconnection, such as a reboot or a laptop waking from sleep. Note that this field is omitted from the response if null or unset.
                      The amount of time in minutes a user is allowed access to their LAN. A value of 0 will allow LAN access until the next WARP reconnection, such as a reboot or a laptop waking from sleep. Note that this field is omitted from the response if null or unset.
                    type: number
                  lanAllowSubnetSize:
                    description: |-
                      (Number) The size of the subnet for the local access network. Note that this field is omitted from the response if null or unset.
                      The size of the subnet for the local access network. Note that this field is omitted from the response if null or unset.
                    type: number
                  match:
                    description: |-
                      (String) The wirefilter expression to match devices. Available values: "identity.email", "identity.groups.id", "identity.groups.name", "identity.groups.email", "identity.service_token_uuid", "identity.saml_attributes", "network", "os.name", "os.version".
                      The wirefilter expression to match devices. Available values: "identity.email", "identity.groups.id", "identity.groups.name", "identity.groups.email", "identity.service_token_uuid", "identity.saml_attributes", "network", "os.name", "os.version".
                    type: string
                  name:
                    description: |-
                      (String) The name of the device settings profile.
                      The name of the device settings profile.
                    type: string
                  precedence:
                    description: |-
                      (Number) The precedence of the policy. Lower values indicate higher precedence. Policies will be evaluated in ascending order of this field.
                      The precedence of the policy. Lower values indicate higher precedence. Policies will be evaluated in ascending order of this field.
                    type: number
                  registerInterfaceIpWithDns:
                    description: |-
                      premises DNS server.
                      Determines if the operating system will register WARP's local interface IP with your on-premises DNS server.
                    type: boolean
                  sccmVpnBoundarySupport:
                    description: |-
                      (Boolean) Determines whether the WARP client indicates to SCCM that it is inside a VPN boundary. (Windows only).
                      Determines whether the WARP client indicates to SCCM that it is inside a VPN boundary. (Windows only).
                    type: boolean
                  serviceModeV2:
                    description: (Attributes) (see below for nested schema)
                    properties:
                      mode:
                        description: |-
                          (String) The mode to run the WARP client under.
                          The mode to run the WARP client under.
                        type: string
                      port:
                        description: |-
                          (Number) The port number when used with proxy mode.
                          The port number when used with proxy mode.
                        type: number
                    type: object
                  supportUrl:
                    description: |-
                      (String) The URL to launch when the Send Feedback button is clicked.
                      The URL to launch when the Send Feedback button is clicked.
                    type: string
                  switchLocked:
                    description: |-
                      (Boolean) Whether to allow the user to turn off the WARP switch and disconnect the client.
                      Whether to allow the user to turn off the WARP switch and disconnect the client.
                    type: boolean
                  tunnelProtocol:
                    description: |-
                      (String) Determines which tunnel protocol to use.
                      Determines which tunnel protocol to use.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.accountId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.accountId)
                || (has(self.initProvider) && has(self.initProvider.accountId))'
            - message: spec.forProvider.match is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.match)
                || (has(self.initProvider) && has(self.initProvider.match))'
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: TrustDeviceCustomProfileStatus defines the observed state
              of TrustDeviceCustomProfile.
            properties:
              atProvider:
                properties:
                  accountId:
                    description: (String)
                    type: string
                  allowModeSwitch:
                    description: |-
                      (Boolean) Whether to allow the user to switch WARP between modes.
                      Whether to allow the user to switch WARP between modes.
                    type: boolean
                  allowUpdates:
                    description: |-
                      (Boolean) Whether to receive update notifications when a new version of the client is available.
                      Whether to receive update notifications when a new version of the client is available.
                    type: boolean
                  allowedToLeave:
                    description: |-
                      (Boolean) Whether to allow devices to leave the organization.
                      Whether to allow devices to leave the organization.
                    type: boolean
                  autoConnect:
                    description: |-
                      (Number) The amount of time in seconds to reconnect after having been disabled.
                      The amount of time in seconds to reconnect after having been disabled.
                    type: number
                  captivePortal:
                    description: |-
                      (Number) Turn on the captive portal after the specified amount of time.
                      Turn on the captive portal after the specified amount of time.
                    type: number
                  default:
                    description: |-
                      (Boolean) Whether the policy is the default policy for an account.
                      Whether the policy is the default policy for an account.
                    type: boolean
                  description:
                    description: |-
                      (String) A description of the policy.
                      A description of the policy.
                    type: string
                  disableAutoFallback:
                    description: |-
                      (Boolean) If the dns_server field of a fallback domain is not present, the client will fall back to a best guess of the default/system DNS resolvers unless this policy option is set to true.
                      If the `dns_server` field of a fallback domain is not present, the client will fall back to a best guess of the default/system DNS resolvers unless this policy option is set to `true`.
                    type: boolean
                  enabled:
                    description: |-
                      (Boolean) Whether the policy will be applied to matching devices.
                      Whether the policy will be applied to matching devices.
                    type: boolean
                  exclude:
                    description: (Attributes List) List of routes excluded in the
                      WARP client's tunnel. Both 'exclude' and 'include' cannot be
                      set in the same request. (see below for nested schema)
                    items:
                      properties:
                        address:
                          description: |-
                            (String) The address in CIDR format to exclude from the tunnel. If address is present, host must not be present.
                            The address in CIDR format to exclude from the tunnel. If `address` is present, `host` must not be present.
                          type: string
                        description:
                          description: |-
                            (String) A description of the policy.
                            A description of the Split Tunnel item, displayed in the client UI.
                          type: string
                        host:
                          description: |-
                            (String) The domain name to exclude from the tunnel. If host is present, address must not be present.
                            The domain name to exclude from the tunnel. If `host` is present, `address` must not be present.
                          type: string
                      type: object
                    type: array
                  excludeOfficeIps:
                    description: |-
                      (Boolean) Whether to add Microsoft IPs to Split Tunnel exclusions.
                      Whether to add Microsoft IPs to Split Tunnel exclusions.
                    type: boolean
                  fallbackDomains:
                    description: (Attributes List) (see below for nested schema)
                    items:
                      properties:
                        description:
                          description: |-
                            (String) A description of the policy.
                            A description of the fallback domain, displayed in the client UI.
                          type: string
                        dnsServer:
                          description: |-
                            (List of String) A list of IP addresses to handle domain resolution.
                            A list of IP addresses to handle domain resolution.
                          items:
                            type: string
                          type: array
                        suffix:
                          description: |-
                            (String) The domain suffix to match when resolving locally.
                            The domain suffix to match when resolving locally.
                          type: string
                      type: object
                    type: array
                  gatewayUniqueId:
                    description: (String)
                    type: string
                  id:
                    description: (String) The ID of this resource.
                    type: string
                  include:
                    description: (Attributes List) List of routes included in the
                      WARP client's tunnel. Both 'exclude' and 'include' cannot be
                      set in the same request. (see below for nested schema)
                    items:
                      properties:
                        address:
                          description: |-
                            (String) The address in CIDR format to exclude from the tunnel. If address is present, host must not be present.
                            The address in CIDR format to include in the tunnel. If `address` is present, `host` must not be present.
                          type: string
                        description:
                          description: |-
                            (String) A description of the policy.
                            A description of the Split Tunnel item, displayed in the client UI.
                          type: string
                        host:
                          description: |-
                            (String) The domain name to exclude from the tunnel. If host is present, address must not be present.
                            The domain name to include in the tunnel. If `host` is present, `address` must not be present.
                          type: string
                      type: object
                    type: array
                  lanAllowMinutes:
                    description: |-
                      (Number) The amount of time in minutes a user is allowed access to their LAN. A value of 0 will allow LAN access until the next WARP reconnection, such as a reboot or a laptop waking from sleep. Note that this field is omitted from the response if null or unset.
                      The amount of time in minutes a user is allowed access to their LAN. A value of 0 will allow LAN access until the next WARP reconnection, such as a reboot or a laptop waking from sleep. Note that this field is omitted from the response if null or unset.
                    type: number
                  lanAllowSubnetSize:
                    description: |-
                      (Number) The size of the subnet for the local access network. Note that this field is omitted from the response if null or unset.
                      The size of the subnet for the local access network. Note that this field is omitted from the response if null or unset.
                    type: number
                  match:
                    description: |-
                      (String) The wirefilter expression to match devices. Available values: "identity.email", "identity.groups.id", "identity.groups.name", "identity.groups.email", "identity.service_token_uuid", "identity.saml_attributes", "network", "os.name", "os.version".
                      The wirefilter expression to match devices. Available values: "identity.email", "identity.groups.id", "identity.groups.name", "identity.groups.email", "identity.service_token_uuid", "identity.saml_attributes", "network", "os.name", "os.version".
                    type: string
                  name:
                    description: |-
                      (String) The name of the device settings profile.
                      The name of the device settings profile.
                    type: string
                  policyId:
                    description: (String)
                    type: string
                  precedence:
                    description: |-
                      (Number) The precedence of the policy. Lower values indicate higher precedence. Policies will be evaluated in ascending order of this field.
                      The precedence of the policy. Lower values indicate higher precedence. Policies will be evaluated in ascending order of this field.
                    type: number
                  registerInterfaceIpWithDns:
                    description: |-
                      premises DNS server.
                      Determines if the operating system will register WARP's local interface IP with your on-premises DNS server.
                    type: boolean
                  sccmVpnBoundarySupport:
                    description: |-
                      (Boolean) Determines whether the WARP client indicates to SCCM that it is inside a VPN boundary. (Windows only).
                      Determines whether the WARP client indicates to SCCM that it is inside a VPN boundary. (Windows only).
                    type: boolean
                  serviceModeV2:
                    description: (Attributes) (see below for nested schema)
                    properties:
                      mode:
                        description: |-
                          (String) The mode to run the WARP client under.
                          The mode to run the WARP client under.
                        type: string
                      port:
                        description: |-
                          (Number) The port number when used with proxy mode.
                          The port number when used with proxy mode.
                        type: number
                    type: object
                  supportUrl:
                    description: |-
                      (String) The URL to launch when the Send Feedback button is clicked.
                      The URL to launch when the Send Feedback button is clicked.
                    type: string
                  switchLocked:
                    description: |-
                      (Boolean) Whether to allow the user to turn off the WARP switch and disconnect the client.
                      Whether to allow the user to turn off the WARP switch and disconnect the client.
                    type: boolean
                  targetTests:
                    description: (Attributes List) (see below for nested schema)
                    items:
                      properties:
                        id:
                          description: |-
                            (String) The ID of this resource.
                            The id of the DEX test targeting this policy.
                          type: string
                        name:
                          description: |-
                            (String) The name of the device settings profile.
                            The name of the DEX test targeting this policy.
                          type: string
                      type: object
                    type: array
                  tunnelProtocol:
                    description: |-
                      (String) Determines which tunnel protocol to use.
                      Determines which tunnel protocol to use.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
