---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: validationrules.token.upjet-cloudflare.m.upbound.io
spec:
  group: token.upjet-cloudflare.m.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - upjet-cloudflare
    kind: ValidationRules
    listKind: ValidationRulesList
    plural: validationrules
    singular: validationrules
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ValidationRules is the Schema for the ValidationRuless API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: ValidationRulesSpec defines the desired state of ValidationRules
            properties:
              forProvider:
                properties:
                  action:
                    description: |-
                      (String) Action to take on requests that match operations included in selector and fail expression.
                      Available values: "log", "block".
                      Action to take on requests that match operations included in `selector` and fail `expression`.
                      Available values: "log", "block".
                    type: string
                  description:
                    description: |-
                      readable description that gives more details than title.
                      A human-readable description that gives more details than `title`.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Toggle rule on or off.
                      Toggle rule on or off.
                    type: boolean
                  expression:
                    description: |-
                      (String) Rule expression. Requests that fail to match this expression will be subject to action.
                      Rule expression. Requests that fail to match this expression will be subject to `action`.

                      For details on expressions, see the [Cloudflare Docs](https://developers.cloudflare.com/api-shield/security/jwt-validation/).
                    type: string
                  position:
                    description: (Attributes) Update rule order among zone rules.
                      (see below for nested schema)
                    properties:
                      after:
                        description: |-
                          (String) Move rule to after rule with this ID.
                          Move rule to after rule with this ID.
                        type: string
                      before:
                        description: |-
                          (String) Move rule to before rule with this ID.
                          Move rule to before rule with this ID.
                        type: string
                      index:
                        description: |-
                          (Number) Move rule to this position
                          Move rule to this position
                        type: number
                    type: object
                  selector:
                    description: (Attributes) Select operations covered by this rule.
                    properties:
                      exclude:
                        description: (Attributes List) Ignore operations that were
                          otherwise included by include. (see below for nested schema)
                        items:
                          properties:
                            operationIds:
                              description: |-
                                (List of String) Excluded operation IDs.
                                Excluded operation IDs.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      include:
                        description: (Attributes List) Select all matching operations.
                          (see below for nested schema)
                        items:
                          properties:
                            host:
                              description: |-
                                (List of String) Included hostnames.
                                Included hostnames.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                    type: object
                  title:
                    description: |-
                      readable name for the rule.
                      A human-readable name for the rule.
                    type: string
                  zoneId:
                    description: |-
                      (String) Identifier.
                      Identifier.
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  action:
                    description: |-
                      (String) Action to take on requests that match operations included in selector and fail expression.
                      Available values: "log", "block".
                      Action to take on requests that match operations included in `selector` and fail `expression`.
                      Available values: "log", "block".
                    type: string
                  description:
                    description: |-
                      readable description that gives more details than title.
                      A human-readable description that gives more details than `title`.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Toggle rule on or off.
                      Toggle rule on or off.
                    type: boolean
                  expression:
                    description: |-
                      (String) Rule expression. Requests that fail to match this expression will be subject to action.
                      Rule expression. Requests that fail to match this expression will be subject to `action`.

                      For details on expressions, see the [Cloudflare Docs](https://developers.cloudflare.com/api-shield/security/jwt-validation/).
                    type: string
                  position:
                    description: (Attributes) Update rule order among zone rules.
                      (see below for nested schema)
                    properties:
                      after:
                        description: |-
                          (String) Move rule to after rule with this ID.
                          Move rule to after rule with this ID.
                        type: string
                      before:
                        description: |-
                          (String) Move rule to before rule with this ID.
                          Move rule to before rule with this ID.
                        type: string
                      index:
                        description: |-
                          (Number) Move rule to this position
                          Move rule to this position
                        type: number
                    type: object
                  selector:
                    description: (Attributes) Select operations covered by this rule.
                    properties:
                      exclude:
                        description: (Attributes List) Ignore operations that were
                          otherwise included by include. (see below for nested schema)
                        items:
                          properties:
                            operationIds:
                              description: |-
                                (List of String) Excluded operation IDs.
                                Excluded operation IDs.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      include:
                        description: (Attributes List) Select all matching operations.
                          (see below for nested schema)
                        items:
                          properties:
                            host:
                              description: |-
                                (List of String) Included hostnames.
                                Included hostnames.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                    type: object
                  title:
                    description: |-
                      readable name for the rule.
                      A human-readable name for the rule.
                    type: string
                  zoneId:
                    description: |-
                      (String) Identifier.
                      Identifier.
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.action is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.action)
                || (has(self.initProvider) && has(self.initProvider.action))'
            - message: spec.forProvider.description is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.description)
                || (has(self.initProvider) && has(self.initProvider.description))'
            - message: spec.forProvider.enabled is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.enabled)
                || (has(self.initProvider) && has(self.initProvider.enabled))'
            - message: spec.forProvider.expression is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.expression)
                || (has(self.initProvider) && has(self.initProvider.expression))'
            - message: spec.forProvider.selector is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.selector)
                || (has(self.initProvider) && has(self.initProvider.selector))'
            - message: spec.forProvider.title is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.title)
                || (has(self.initProvider) && has(self.initProvider.title))'
            - message: spec.forProvider.zoneId is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.zoneId)
                || (has(self.initProvider) && has(self.initProvider.zoneId))'
          status:
            description: ValidationRulesStatus defines the observed state of ValidationRules.
            properties:
              atProvider:
                properties:
                  action:
                    description: |-
                      (String) Action to take on requests that match operations included in selector and fail expression.
                      Available values: "log", "block".
                      Action to take on requests that match operations included in `selector` and fail `expression`.
                      Available values: "log", "block".
                    type: string
                  createdAt:
                    description: (String)
                    type: string
                  description:
                    description: |-
                      readable description that gives more details than title.
                      A human-readable description that gives more details than `title`.
                    type: string
                  enabled:
                    description: |-
                      (Boolean) Toggle rule on or off.
                      Toggle rule on or off.
                    type: boolean
                  expression:
                    description: |-
                      (String) Rule expression. Requests that fail to match this expression will be subject to action.
                      Rule expression. Requests that fail to match this expression will be subject to `action`.

                      For details on expressions, see the [Cloudflare Docs](https://developers.cloudflare.com/api-shield/security/jwt-validation/).
                    type: string
                  id:
                    description: (String) UUID.
                    type: string
                  lastUpdated:
                    description: (String)
                    type: string
                  position:
                    description: (Attributes) Update rule order among zone rules.
                      (see below for nested schema)
                    properties:
                      after:
                        description: |-
                          (String) Move rule to after rule with this ID.
                          Move rule to after rule with this ID.
                        type: string
                      before:
                        description: |-
                          (String) Move rule to before rule with this ID.
                          Move rule to before rule with this ID.
                        type: string
                      index:
                        description: |-
                          (Number) Move rule to this position
                          Move rule to this position
                        type: number
                    type: object
                  selector:
                    description: (Attributes) Select operations covered by this rule.
                    properties:
                      exclude:
                        description: (Attributes List) Ignore operations that were
                          otherwise included by include. (see below for nested schema)
                        items:
                          properties:
                            operationIds:
                              description: |-
                                (List of String) Excluded operation IDs.
                                Excluded operation IDs.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                      include:
                        description: (Attributes List) Select all matching operations.
                          (see below for nested schema)
                        items:
                          properties:
                            host:
                              description: |-
                                (List of String) Included hostnames.
                                Included hostnames.
                              items:
                                type: string
                              type: array
                          type: object
                        type: array
                    type: object
                  title:
                    description: |-
                      readable name for the rule.
                      A human-readable name for the rule.
                    type: string
                  zoneId:
                    description: |-
                      (String) Identifier.
                      Identifier.
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
