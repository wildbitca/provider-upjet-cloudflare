---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: oauthclients.account.upjet-cloudflare.upbound.io
spec:
  group: account.upjet-cloudflare.upbound.io
  names:
    categories:
    - crossplane
    - managed
    - upjet-cloudflare
    kind: OAuthClient
    listKind: OAuthClientList
    plural: oauthclients
    singular: oauthclient
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: OAuthClient is the Schema for the OAuthClients API. Accepted
          Permissions OAuth Client ReadOAuth Client Write
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: OAuthClientSpec defines the desired state of OAuthClient
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                properties:
                  accountId:
                    description: |-
                      (String) Account identifier tag.
                      Account identifier tag.
                    type: string
                  accountIdRef:
                    description: Reference to a Account in account to populate accountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  accountIdSelector:
                    description: Selector for a Account in account to populate accountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  allowedCorsOrigins:
                    description: |-
                      (List of String) Array of allowed CORS origins.
                      Array of allowed CORS origins.
                    items:
                      type: string
                    type: array
                  clientName:
                    description: |-
                      readable name of the OAuth client.
                      Human-readable name of the OAuth client.
                    type: string
                  clientUri:
                    description: |-
                      (String) URL of the home page of the client.
                      URL of the home page of the client.
                    type: string
                  grantTypes:
                    description: |-
                      (List of String) Array of OAuth grant types the client is allowed to use. authorization_code is required; refresh_token may be included optionally.
                      Array of OAuth grant types the client is allowed to use. `authorization_code` is required; `refresh_token` may be included optionally.
                    items:
                      type: string
                    type: array
                  logoUri:
                    description: |-
                      (String) URL of the client's logo.
                      URL of the client's logo.
                    type: string
                  oauthClientId:
                    description: |-
                      (String) The unique identifier for an OAuth client.
                      The unique identifier for an OAuth client.
                    type: string
                  policyUri:
                    description: |-
                      (String) URL that points to a privacy policy document.
                      URL that points to a privacy policy document.
                    type: string
                  postLogoutRedirectUris:
                    description: |-
                      logout redirect URIs.
                      Array of allowed post-logout redirect URIs.
                    items:
                      type: string
                    type: array
                  redirectUris:
                    description: |-
                      (List of String) Array of allowed redirect URIs for the client.
                      Array of allowed redirect URIs for the client.
                    items:
                      type: string
                    type: array
                  responseTypes:
                    description: |-
                      (List of String) Array of OAuth response types the client is allowed to use.
                      Array of OAuth response types the client is allowed to use.
                    items:
                      type: string
                    type: array
                  scopes:
                    description: |-
                      delimited scopes are not accepted. Dot-delimited scopes are validated against available OAuth API scopes; simple identity scopes are allowed. Protocol scopes offline_access and openid are added or removed automatically based on grant_types and response_types.
                      Array of OAuth scopes the client is allowed to request. Colon-delimited scopes are not accepted. Dot-delimited scopes are validated against available OAuth API scopes; simple identity scopes are allowed. Protocol scopes `offline_access` and `openid` are added or removed automatically based on `grant_types` and `response_types`.
                    items:
                      type: string
                    type: array
                  tokenEndpointAuthMethod:
                    description: |-
                      (String) The authentication method the client uses at the token endpoint.
                      Available values: "none", "client_secret_basic", "client_secret_post".
                      The authentication method the client uses at the token endpoint.
                      Available values: "none", "client_secret_basic", "client_secret_post".
                    type: string
                  tosUri:
                    description: |-
                      (String) URL that points to a terms of service document.
                      URL that points to a terms of service document.
                    type: string
                  visibility:
                    description: |-
                      empty client name, logo URI, verified client URI host, and at least one non-identity scope.
                      Available values: "public".
                      Promote the OAuth client from private to public visibility. Only `public` is accepted; demotion to `private` is not supported. Promotion requires a non-empty client name, logo URI, verified client URI host, and at least one non-identity scope.
                      Available values: "public".
                    type: string
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  accountId:
                    description: |-
                      (String) Account identifier tag.
                      Account identifier tag.
                    type: string
                  accountIdRef:
                    description: Reference to a Account in account to populate accountId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  accountIdSelector:
                    description: Selector for a Account in account to populate accountId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  allowedCorsOrigins:
                    description: |-
                      (List of String) Array of allowed CORS origins.
                      Array of allowed CORS origins.
                    items:
                      type: string
                    type: array
                  clientName:
                    description: |-
                      readable name of the OAuth client.
                      Human-readable name of the OAuth client.
                    type: string
                  clientUri:
                    description: |-
                      (String) URL of the home page of the client.
                      URL of the home page of the client.
                    type: string
                  grantTypes:
                    description: |-
                      (List of String) Array of OAuth grant types the client is allowed to use. authorization_code is required; refresh_token may be included optionally.
                      Array of OAuth grant types the client is allowed to use. `authorization_code` is required; `refresh_token` may be included optionally.
                    items:
                      type: string
                    type: array
                  logoUri:
                    description: |-
                      (String) URL of the client's logo.
                      URL of the client's logo.
                    type: string
                  oauthClientId:
                    description: |-
                      (String) The unique identifier for an OAuth client.
                      The unique identifier for an OAuth client.
                    type: string
                  policyUri:
                    description: |-
                      (String) URL that points to a privacy policy document.
                      URL that points to a privacy policy document.
                    type: string
                  postLogoutRedirectUris:
                    description: |-
                      logout redirect URIs.
                      Array of allowed post-logout redirect URIs.
                    items:
                      type: string
                    type: array
                  redirectUris:
                    description: |-
                      (List of String) Array of allowed redirect URIs for the client.
                      Array of allowed redirect URIs for the client.
                    items:
                      type: string
                    type: array
                  responseTypes:
                    description: |-
                      (List of String) Array of OAuth response types the client is allowed to use.
                      Array of OAuth response types the client is allowed to use.
                    items:
                      type: string
                    type: array
                  scopes:
                    description: |-
                      delimited scopes are not accepted. Dot-delimited scopes are validated against available OAuth API scopes; simple identity scopes are allowed. Protocol scopes offline_access and openid are added or removed automatically based on grant_types and response_types.
                      Array of OAuth scopes the client is allowed to request. Colon-delimited scopes are not accepted. Dot-delimited scopes are validated against available OAuth API scopes; simple identity scopes are allowed. Protocol scopes `offline_access` and `openid` are added or removed automatically based on `grant_types` and `response_types`.
                    items:
                      type: string
                    type: array
                  tokenEndpointAuthMethod:
                    description: |-
                      (String) The authentication method the client uses at the token endpoint.
                      Available values: "none", "client_secret_basic", "client_secret_post".
                      The authentication method the client uses at the token endpoint.
                      Available values: "none", "client_secret_basic", "client_secret_post".
                    type: string
                  tosUri:
                    description: |-
                      (String) URL that points to a terms of service document.
                      URL that points to a terms of service document.
                    type: string
                  visibility:
                    description: |-
                      empty client name, logo URI, verified client URI host, and at least one non-identity scope.
                      Available values: "public".
                      Promote the OAuth client from private to public visibility. Only `public` is accepted; demotion to `private` is not supported. Promotion requires a non-empty client name, logo URI, verified client URI host, and at least one non-identity scope.
                      Available values: "public".
                    type: string
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.clientName is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.clientName)
                || (has(self.initProvider) && has(self.initProvider.clientName))'
            - message: spec.forProvider.grantTypes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.grantTypes)
                || (has(self.initProvider) && has(self.initProvider.grantTypes))'
            - message: spec.forProvider.redirectUris is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.redirectUris)
                || (has(self.initProvider) && has(self.initProvider.redirectUris))'
            - message: spec.forProvider.responseTypes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.responseTypes)
                || (has(self.initProvider) && has(self.initProvider.responseTypes))'
            - message: spec.forProvider.scopes is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.scopes)
                || (has(self.initProvider) && has(self.initProvider.scopes))'
            - message: spec.forProvider.tokenEndpointAuthMethod is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.tokenEndpointAuthMethod)
                || (has(self.initProvider) && has(self.initProvider.tokenEndpointAuthMethod))'
          status:
            description: OAuthClientStatus defines the observed state of OAuthClient.
            properties:
              atProvider:
                properties:
                  accountId:
                    description: |-
                      (String) Account identifier tag.
                      Account identifier tag.
                    type: string
                  allowedCorsOrigins:
                    description: |-
                      (List of String) Array of allowed CORS origins.
                      Array of allowed CORS origins.
                    items:
                      type: string
                    type: array
                  clientId:
                    description: |-
                      (String) The unique identifier for an OAuth client.
                      The unique identifier for an OAuth client.
                    type: string
                  clientName:
                    description: |-
                      readable name of the OAuth client.
                      Human-readable name of the OAuth client.
                    type: string
                  clientUri:
                    description: |-
                      (String) URL of the home page of the client.
                      URL of the home page of the client.
                    type: string
                  clientUriVerification:
                    description: (Attributes) Client URI domain control verification
                      state. (see below for nested schema)
                    properties:
                      status:
                        description: |-
                          (String) Current verification status for the client URI host.
                          Available values: "pending", "in_progress", "verified", "failed".
                          Current verification status for the client URI host.
                          Available values: "pending", "in_progress", "verified", "failed".
                        type: string
                      text:
                        description: |-
                          (String) Exact TXT record value that must be added to DNS to prove ownership of the client URI host.
                          Exact TXT record value that must be added to DNS to prove ownership of the client URI host.
                        type: string
                    type: object
                  createdAt:
                    description: |-
                      (String) Timestamp when the OAuth client was created.
                      Timestamp when the OAuth client was created.
                    type: string
                  grantTypes:
                    description: |-
                      (List of String) Array of OAuth grant types the client is allowed to use. authorization_code is required; refresh_token may be included optionally.
                      Array of OAuth grant types the client is allowed to use. `authorization_code` is required; `refresh_token` may be included optionally.
                    items:
                      type: string
                    type: array
                  hasRotatedSecret:
                    description: |-
                      (Boolean) Indicates whether the client has a rotated secret that has not yet been deleted.
                      Indicates whether the client has a rotated secret that has not yet been deleted.
                    type: boolean
                  id:
                    type: string
                  logoUri:
                    description: |-
                      (String) URL of the client's logo.
                      URL of the client's logo.
                    type: string
                  oauthClientId:
                    description: |-
                      (String) The unique identifier for an OAuth client.
                      The unique identifier for an OAuth client.
                    type: string
                  policyUri:
                    description: |-
                      (String) URL that points to a privacy policy document.
                      URL that points to a privacy policy document.
                    type: string
                  postLogoutRedirectUris:
                    description: |-
                      logout redirect URIs.
                      Array of allowed post-logout redirect URIs.
                    items:
                      type: string
                    type: array
                  promotedAt:
                    description: |-
                      (String) Timestamp when the OAuth client was promoted to public visibility.
                      Timestamp when the OAuth client was promoted to public visibility.
                    type: string
                  redirectUris:
                    description: |-
                      (List of String) Array of allowed redirect URIs for the client.
                      Array of allowed redirect URIs for the client.
                    items:
                      type: string
                    type: array
                  responseTypes:
                    description: |-
                      (List of String) Array of OAuth response types the client is allowed to use.
                      Array of OAuth response types the client is allowed to use.
                    items:
                      type: string
                    type: array
                  scopes:
                    description: |-
                      delimited scopes are not accepted. Dot-delimited scopes are validated against available OAuth API scopes; simple identity scopes are allowed. Protocol scopes offline_access and openid are added or removed automatically based on grant_types and response_types.
                      Array of OAuth scopes the client is allowed to request. Colon-delimited scopes are not accepted. Dot-delimited scopes are validated against available OAuth API scopes; simple identity scopes are allowed. Protocol scopes `offline_access` and `openid` are added or removed automatically based on `grant_types` and `response_types`.
                    items:
                      type: string
                    type: array
                  tokenEndpointAuthMethod:
                    description: |-
                      (String) The authentication method the client uses at the token endpoint.
                      Available values: "none", "client_secret_basic", "client_secret_post".
                      The authentication method the client uses at the token endpoint.
                      Available values: "none", "client_secret_basic", "client_secret_post".
                    type: string
                  tosUri:
                    description: |-
                      (String) URL that points to a terms of service document.
                      URL that points to a terms of service document.
                    type: string
                  updatedAt:
                    description: |-
                      (String) Timestamp when the OAuth client was last updated.
                      Timestamp when the OAuth client was last updated.
                    type: string
                  visibility:
                    description: |-
                      empty client name, logo URI, verified client URI host, and at least one non-identity scope.
                      Available values: "public".
                      Promote the OAuth client from private to public visibility. Only `public` is accepted; demotion to `private` is not supported. Promotion requires a non-empty client name, logo URI, verified client URI host, and at least one non-identity scope.
                      Available values: "public".
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
